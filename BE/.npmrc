# next-auth (beta) mendeklarasikan peer OPSIONAL nodemailer ^7 || ^8 untuk provider Email yang tidak kita pakai;
# kita memakai nodemailer 10 langsung untuk email reset password (advisory keamanan dipatch di 10.x).
legacy-peer-deps=true
