cd mqtt
touch config/password_file
docker run -it --rm \
  -v "$(pwd)/config:/mosquitto/config" \
  eclipse-mosquitto \
  mosquitto_passwd -b /mosquitto/config/password_file esp32_device "BMSADB123!"

# tambah user lain (mis. backend subscriber) dengan flag -b tanpa -c:
docker run -it --rm \
  -v "$(pwd)/config:/mosquitto/config" \
  eclipse-mosquitto \
  mosquitto_passwd -b /mosquitto/config/password_file backend_service "BMSADB123!"